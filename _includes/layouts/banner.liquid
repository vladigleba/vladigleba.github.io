<header class="banner">
  {% if show_blur_bg %}
    <div class="blur-bg" style="background: {{ glow_color }};"></div>
  {% endif %}
  <hgroup class="intro">
    <h1>{{ title }}<br><span>{{ subtitle }}</span></h1>
    <p>{{ description }}</p>
  </hgroup>
</header>
